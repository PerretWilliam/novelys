# Novelys Mobile

Novelys mobile app (Expo + React Native).

## Scripts

```bash
pnpm dev
pnpm ios
pnpm android
pnpm typecheck
```

## Configuration

Copy `.env.example` to `.env`:

- `EXPO_PUBLIC_API_BASE_URL`
- `EXPO_PUBLIC_API_TOKEN`
